import Content from '@layouts/Container';
import React, { memo } from 'react';

const NotiContainer = () => {
  return (
    <Content>
      noti 
    </Content>
  );
};

export default memo(NotiContainer);