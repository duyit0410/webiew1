import Content from '@layouts/Content';
import React, { memo } from 'react';

const NotiContainer = () => {
  return (
    <Content>
      noti 
    </Content>
  );
};

export default memo(NotiContainer);